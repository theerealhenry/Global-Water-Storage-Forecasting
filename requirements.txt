# Pinned runtime dependencies, verified compatible for Python 3.11 (checked against
# PyPI release metadata + requires_dist constraints on 2026-08-10 — see docs/adr/0003-*.md
# for the full compatibility trace, including the one real conflict found and resolved).

# Core scientific stack
numpy==2.4.6
scipy==1.17.1
pandas==2.3.3
scikit-learn==1.9.0
pyarrow==24.0.0

# Gradient boosting
lightgbm==4.7.0
xgboost==3.2.0

# Experiment tracking / hyperparameter search
mlflow==3.15.1
optuna==4.9.0

# Interpretability (SHAP pulls in numba/llvmlite — versions pinned to satisfy numba's
# numpy<2.5 ceiling)
shap==0.51.0
numba==0.66.0
llvmlite==0.48.0

# Serving / app layer
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
streamlit==1.61.1

# Visualization
matplotlib==3.11.1
seaborn==0.13.2

# Utilities
pyyaml==6.0.3
python-dotenv==1.2.2
tqdm==4.70.0
