# Base project configuration — Project Phase 2 onward.
#
# Loaded by src/tws_forecast/utils/config.py. Values here are project-wide
# defaults, verified directly against docs/DATA_DICTIONARY.md and
# notebooks/02_forecastability.ipynb (see
# src/tws_forecast/validation/phase1_constants.py for the full, cited set of
# Phase 1 measurements). Scenario-specific validation configs live
# separately under configs/validation/ (Phase 2 step 2.5) and reference
# random_seed from here rather than repeating it.

random_seed: 42

data_dir: data/raw

train_period:
  start: "2002-05-01"
  end: "2015-08-01"

test_period:
  start: "2015-09-01"
  end: "2018-12-01"
